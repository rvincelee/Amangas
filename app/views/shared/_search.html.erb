<%= form_with(url: search_path, method: "get") do |form| %>
  <%= form.text_field :keyword, placeholder: "Search by keyword" %>
  <%= form.collection_select :genre_id, Genre.all, :id, :name, { prompt: "Select Genre" } %>
  <%= form.submit "Search" %>
<% end %>